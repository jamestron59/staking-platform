[project]
name = "hlq"
version = "0.1.0"
description = "Hyperliquid systematic trading stack"
requires-python = ">=3.11"

[project.scripts]
hlq = "hlq.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
